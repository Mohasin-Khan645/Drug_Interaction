# ==============================================================================
# DrugSafe Clinical Platform - Frontend Production Environment Configuration
# ==============================================================================

# API Gateway Base URL (Defaults to relative /api when proxied by Nginx)
VITE_API_BASE_URL=/api

# Disable Client Mock API in Production (Authoritative Backend Verification)
VITE_USE_MOCK_API=false
